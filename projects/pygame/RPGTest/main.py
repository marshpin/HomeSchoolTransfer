import pygame 
import pillow as pil
from pygame import QUIT, KEYDOWN, rect, Rect

screenSize = (400,400)
screen = pygame.display.set_mode(screenSize)
pygame.display.set_caption("Game Window")
pygame.display.set_icon()